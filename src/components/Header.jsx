import Styles from './Styles.module.scss';

const Header = () => {
  return (
    <header className={Styles.header}>
      <h1>
        Hello world!
      </h1>
    </header>
  );
};

export default Header;
